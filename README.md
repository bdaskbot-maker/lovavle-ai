# SST repository workspace

This repository contains the SST source archive merged into the Vercel Next.js workspace.

- `sst-dev/` — SST CLI, platform, examples, and documentation source.
- The root Next.js app remains available as the Vercel preview surface.

The original SST documentation deployment configuration is preserved at `sst-dev/vercel.json`.
